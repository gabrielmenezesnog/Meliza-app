import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {
    padding: 32,
    position: 'relative',
  },
  modalContainer: {},
  profileHeader: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
  },
});
